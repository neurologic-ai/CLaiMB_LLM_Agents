{
  "aml.jobs_flow_band": [
    "Success at 0.94 is the limiter → add retry-on-transient and resource quota checks → raise success_rate to ≥0.95 (unlocks band 4).",
    "Lead-time 6.4h acceptable but improvable → enable caching and artifact reuse → target ≤4h (pushes toward band 5 when success is ≥0.98)."
  ],
  "aml.registry_governance_band": [
    "Median transition time at 60h prevents band-5 → parallelize QA and security gates → reduce median_transition_h to <48h (unlocks band 5).",
    "Staging coverage at 0.83 <0.90 → enforce stage on register policy → raise pct_staged to ≥0.90 (supports band 5)."
  ],
  "cicd.deploy_frequency_band": [
    "per-service rate is below threshold → increase deployments or reduce services → raise freq_per_week to ≥6 (unlocks band 5)"
  ],
  "cicd.lead_time_band": [
    "p50 at 6.8h prevents band-5 → shard tests and cache dependencies → reduce p50 ≤4h (unlocks band 5)."
  ],
  "mlflow.experiment_completeness_band": [
    "Artifacts at 0.82 cap the band → persist model files and evaluation reports on every run → raise pct_artifacts to ≥0.85 (unlocks band 4)."
  ],
  "mlflow.lineage_coverage_band": [
    "Data references at 0.86 block a higher band → log dataset version/hash via a pre-run hook → raise pct_data_ref to ≥0.90 (unlocks band 5)."
  ],
  "mlflow.registry_hygiene_band": [
    "One recent rollback limits confidence → add pre-deploy shadow tests and tighten rollback criteria → keep rollbacks_30d at 0 (unlocks band 5)."
  ],
  "mlflow.reproducibility_band": [
    "Signature conflict on 'sigA' → review model signature management and ensure consistency → eliminate conflicts and raise match_rate ≥0.95 (unlocks band 5)."
  ],
  "mlflow.validation_artifacts_band": [
    "SHAP coverage at 0.84 → implement SHAP file generation in all runs → raise pct_with_shap to ≥0.85 (unlocks band 4).",
    "Bias report at 0.72 → integrate a bias evaluation step in the pipeline → raise pct_with_bias_report to ≥0.80 (unlocks band 4)."
  ],
  "sm.experiments_lineage_band": [
    "Data ref at 0.86 blocks band-5 → log immutable dataset version or hash per run → raise pct_data_ref to ≥0.90 (unlocks band 5)."
  ],
  "sm.pipeline_flow_band": [
    "Promotion latency at 12h → automate risk sign-off and bake validation into CI → reduce promotion_time_h to ≤8h (unlocks band 5).",
    "p95 at 42min → parallelize feature gen and cache heavy steps → keep p95 ≤30min (reinforces band 5)."
  ],
  "sm.endpoint_slo_scaling_band": [
    "Max RPS at 950 < 800 → optimize resource allocation and load balancing → raise max_rps_at_slo to ≥800 (unlocks band 5)."
  ]
}